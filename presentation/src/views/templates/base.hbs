<!DOCTYPE html>
<html>
    <head>
        <title>{{title}}</title>

        {{! styles }}
        <link rel="stylesheet" href="{{ css 'global' }}">
        {{#each style}}
        <link rel="stylesheet" href="{{ css this }}">
        {{/each}}
    </head>

    <body>
        {{> header }}
        {{> body }}
        {{> footer }}

        {{! scripts }}
        {{#dev}}
        <script src="http://localhost:{{ config.ports.watch }}/livereload.js"></script>
        {{/dev}}
    </body>
</html>